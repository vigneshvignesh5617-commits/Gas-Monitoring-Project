import React from 'react';

// NGODashboard removed — kept placeholder for compatibility.
const NGODashboard = () => (
  <div className="min-h-screen flex items-center justify-center">
    <div className="text-center p-8">
      <h2 className="text-2xl font-bold">Partner Portal Removed</h2>
      <p className="text-gray-600 mt-2">This site now focuses on Smart LPG monitoring; NGO verification features were removed.</p>
    </div>
  </div>
);

export default NGODashboard;